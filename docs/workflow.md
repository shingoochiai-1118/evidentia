# 新しい情報の追加ワークフロー

情報の追加には2つの経路がある。

1. **都度手動**: 気になった記事をその場でClaudeに評価してもらい `data/entries.json` に直接追加(下記)
2. **週次の半自動更新**: 毎週日曜8:00に自動実行されるリサーチが `data/candidates.json` に候補を溜め、
   ユーザーが確認して採用したものだけを `data/entries.json` に昇格させる([[#週次の半自動更新]]参照)

いずれの経路でも、エントリーの評価基準・スキーマ・信頼できるソースの扱いは共通([[evidence-rating]]参照)。

## リサーチ対象の全体像

Evidentiaが集める情報は、大きく2方向に分かれる。どちらも同じ基準・スキーマで扱う。

**① 摂るべき・行うべきもの(予防・改善に役立つもの)**
- 運動: 種類(有酸素運動・筋トレ・ストレッチ等)、強度、頻度、タイミングまで具体的に扱う。
  可能な限り数値を伴う推奨まで踏み込む(例: 「週150〜300分」)。
- 食事: カロリー、頻度・回数・形式(間欠的断食等)、栄養バランスに加え、以下の食品カテゴリを
  意識的にカバーする。可能な限り推奨摂取量まで踏み込む(例: 「魚1日20g」「加工肉1日50gごとに+18%」)。
  - 野菜・食物繊維、果物、魚、肉、酒、乳製品、海藻、発酵食品、菓子、飲料(茶・コーヒー等含む)
- 睡眠: 推奨時間、就寝・起床のタイミング(概日リズム)。children(子ども向け)カテゴリでは
  乳幼児・幼児期の推奨睡眠時間・タイミングも対象に含む。
- サプリメント: 成分・作用機序が明確なもののみを対象とする。成分が曖昧な「怪しい」サプリメントは
  低い星で載せるのではなく、そもそもリサーチ・追加の対象外とする
- 医療の専門知識: 新しい薬剤、既存薬剤の新しい使い方(適応拡大等、例: GLP-1受容体作動薬の肥満治療への
  応用)。処方薬レベルの情報は「医師の判断が前提」であることを`caution`に明記する。情報の新しさは
  信頼度(★の数)とは別軸であり、古い研究・ガイドラインでも覆っていなければ星を下げる理由にはならない
  ([[evidence-rating]]の「情報の鮮度について」参照)。

**② 避けるべきもの・習慣(リスクを上げるもの)**
- 食品・飲料(例: 加工肉、過度な飲酒)、生活習慣(例: 座りすぎ、喫煙)など、①と対称的にリスクを
  上げる側の情報も同じ重みで収集する

**③ 通説の検証(①②どちらの方向でも起こりうる)**
- 「良いとされてきたが実は証拠が弱い/覆った」→ `status: "myth_revised"`
- 「良い面・悪い面の両方の証拠があり結論が割れている」→ `status: "under_debate"`

対象の7カテゴリと、カテゴリごとの範囲(予防のみか改善・対策も含むか)は下記の「Claude側の手順」を参照。

## 使い方(ユーザー側・都度手動)

気になる記事・投稿・論文を見つけたら、Claude Code をこのプロジェクトフォルダ(`claude_code/`)で
起動して、次のように頼むだけでよい。

```
この記事を評価してEvidentiaに追加して: <URLまたは記事の内容>
```

複数まとめて渡してもよい。カテゴリを指定したい場合(例: 「これは頭痛カテゴリで」)は一言添える。

## Claude側の手順(このプロジェクトで新規エントリーを追加する際に従うこと)

1. 元記事・投稿が「一次情報」か「二次情報」かを見極める。二次情報(ニュース記事、SNS投稿)の場合は、
   引用元の一次情報(論文・ガイドライン)まで遡って確認する。見つからない場合は `caution` にその旨を明記し、
   星は保守的に付ける。
2. `docs/evidence-rating.md` の基準に沿って星(1〜5)と `evidence_level` を決める。
3. `data/categories.json` にある7カテゴリ(cancer / cardio / obesity / antiaging / musculoskeletal / headache / children)
   のいずれかに分類する。当てはまらない場合はユーザーに確認する。
   **注意**: antiaging(アンチエイジング)・musculoskeletal(肩こり・腰痛)・headache(頭痛)の3カテゴリは、
   厳密な「予防」だけでなく「改善・対策」のアプローチも対象に含む。この3カテゴリで情報を探す/評価する際は、
   「発症を防ぐ」エビデンスだけに絞らず、「すでにある症状を改善する」エビデンスも同様に価値があるものとして扱う。
   headache(頭痛)カテゴリは片頭痛(migraine)と緊張性頭痛(tension-type headache)の両方を対象とする。
   children(子ども向け)カテゴリは乳幼児〜幼児向けの情報をまとめて扱う。「アレルギー予防」「感染症予防」等の
   サブテーマは別カテゴリに分けず、`tags`(例: "アレルギー予防", "感染症予防")で区別する
   (headacheでmigraine/tension-typeをtagsで区別するのと同じ考え方)。
   **食習慣の範囲について**: 食事関連のリサーチは「何を食べるか」(食品・栄養素)だけでなく、「いつ・どのくらいの
   頻度で食べるか」(食事のタイミング、食事回数、間欠的断食、朝食欠食の影響など)も同様に価値のある対象とする。
   特定の食品(ブロッコリー、魚、玉ねぎ等)を個別に評価してもよいし、食事パターン全体(地中海食等)を
   評価してもよい。「体に良いとされる食品」ほど過大評価されがちなので、がん種・対象疾患ごとにエビデンスの
   強さが異なる点(例: ある食品ではがん種によって結果が割れる)を丁寧に扱う。
   **避けるべきもの・習慣について**: 「摂るべき・行うべきもの」だけでなく、リスクを上げる食品・飲料・
   習慣(加工肉、過度な飲酒、座りすぎ等)も同じ重みでリサーチ対象とする(詳細は「リサーチ対象の全体像」参照)。
4. `summary` は必ず3段階(basic / general / professional)で書く。同じ事実を、複雑さのレベルだけを変えて
   表現する(内容が食い違ってはいけない)。
   - **basic(中学生向け)**: 専門用語を一切使わない。「メタ解析」「RCT」等の代わりに「たくさんの研究をまとめて調べた」
     のように言い換える。可能なら身近な例えを入れる。
   - **general(学生・一般向け)**: 専門用語は最小限。現在サイトのデフォルト表示レベル。
   - **professional(専門職向け)**: 実際の医学用語・略語(RCT, HR, メタ解析, 学会名とグレード等)を使い、
     数値(N数、追跡期間、相対リスク等)を含めて簡潔に。
5. `data/entries.json` に以下のスキーマでエントリーを追記する(配列の末尾に追加でよい、並び順はサイト側で制御される)。

```json
{
  "id": "kebab-case-の一意なID",
  "title": "結論が一目でわかる日本語タイトル",
  "category": "cancer | cardio | obesity | antiaging | musculoskeletal | headache | children",
  "tags": ["タグ1", "タグ2"],
  "stars": 1〜5,
  "status": "well_established | myth_revised | under_debate",
  "evidence_level": "根拠の種類を短く(例: 大規模RCT, メタ解析, 学会ガイドラインLevel B)",
  "summary": {
    "basic": "中学生向け。専門用語を一切使わず、平易な言葉と身近な例えで2〜4文",
    "general": "学生・一般向け。専門用語を控えめにし、理解しやすい文章で2〜4文",
    "professional": "専門職向け。専門用語(RCT/メタ解析/HR/学会名等)を用いて簡潔に2〜3文"
  },
  "practical_takeaway": "生活にどう活かすかを1〜2文で",
  "caution": "注意点・限界・副作用など。無ければ空文字",
  "sources": [{ "name": "出典名", "url": "実在するURL", "type": "guideline|RCT|meta_analysis|cohort|expert_opinion|patient_guideline" }],
  "date_added": "YYYY-MM-DD",
  "last_reviewed": "YYYY-MM-DD"
}
```

6. `sources.url` は必ず実在確認したURLのみを使う。不確かな場合はWeb検索で一次情報を確認してから追加する。
   検索対象ドメイン・検索言語の方針は [[evidence-rating]] の「優先する情報源」「検索言語の方針」を参照。
   英語で一次エビデンスを確認した上で、検診年齢や薬剤の国内承認状況など日本固有の差分がありそうな
   トピックは、日本語でも(厚労省・国立がん研究センター・各専門学会などを)確認する。
7. 追加後、`site/index.html` をブラウザで開いて表示を確認する(ビルド不要、JSONを直接読み込む)。サイト上部の
   「解説レベル」トグルで basic/general/professional の3表示を切り替えて確認する。
8. エントリーが特定の食品・運動(例: コーヒー、ヨガ)を主題にしている場合、`data/items.json` にその項目が
   無ければ追加を検討する(`site/items.html`「食品・運動別」ページに表示される)。`matchTags` にはエントリーの
   `tags` と一致する文字列を入れる。必須ではないが、追加しておくと一覧ページの空欄が自然に埋まる。

## 週次の半自動更新

毎週日曜8:00(Asia/Tokyo)にスケジュール済みのClaudeエージェントが本リポジトリで自動実行され、
以下を行う。

1. `data/entries.json` と `data/candidates.json` を読み、既存エントリーと重複しないテーマを選ぶ。
   7カテゴリ(cancer / cardio / obesity / antiaging / musculoskeletal / headache / children)のうち、
   まだ手薄なカテゴリ([[roadmap]] Phase 1参照)を優先する。
2. Web検索で各カテゴリにつき1〜3件、一次情報(ガイドライン改定・RCT・メタ解析)ベースの候補を探す。
   検索対象ドメイン・検索言語の方針は [[evidence-rating]] を参照(英語を主軸に、検診年齢や薬剤の
   国内承認状況など日本固有の差分がありそうなトピックは日本語でも確認する)。
   二次情報しか見つからない場合はその旨を明記し、無理に候補化しない(0件でもよい)。
3. 見つけた候補を `docs/evidence-rating.md` の基準で評価し、`data/entries.json` と同じスキーマに
   `"proposed_date": "YYYY-MM-DD"` を加えた形で `data/candidates.json` に追記する
   (この時点では `data/entries.json` には書き込まない)。
4. 追加した候補の一覧(タイトル・星・一行要約)を短くまとめる。
5. 変更内容(`data/candidates.json` の更新)をコミットしてpushする。GitHub Pages上のサイトには
   まだ表示させたくないため、候補である間は `site/app.js` からは読み込まない(現状のまま)。
6. 最後に必ずPushNotificationツールで実行結果(件数・カテゴリ)を通知する。デスクトップ通知、
   Remote Control連携時はスマホにもプッシュされる。候補が0件の場合もその旨を通知する。

### ユーザーによるレビュー・採用

ユーザーは週明けなど都合の良いタイミングで、Claudeに次のように頼む。

```
今週の候補を確認して
```

Claudeは `data/candidates.json` の内容を提示し、ユーザーが「1番と3番を採用」のように答えたら、
該当エントリーから `proposed_date` を除いて `data/entries.json` に移し、`data/candidates.json` から
削除し、コミット・pushしてサイトに反映する。不採用のものは理由を一言添えて `data/candidates.json`
から削除する。

## エントリーの削除・修正

- **削除したい場合**: 「〇〇の記事を削除して」とタイトルやトピックを伝えるだけでよい。該当エントリーを
  `data/entries.json` から取り除き、コミット・pushする。
- **エビデンスが覆った場合**: エントリーを削除するのではなく、`status`を`"myth_revised"`に変更し、
  `caution`に何が・いつ・どう変わったかを追記し、`last_reviewed`を更新する形で対応する
  (詳細は [[evidence-rating]] の「エビデンスが覆った場合の扱い」参照)。

## 将来の自動化(Phase 4以降)

週次更新が安定して回るようになったら、カテゴリ別の頻度調整(手薄なカテゴリは頻度を上げる等)や、
`last_reviewed` が古いエントリーの再検証を自動リサーチに組み込むことを検討する。
